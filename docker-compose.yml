services:
  nginx:
    image: ghcr.io/mariosangut/mariosangut.com:latest
    container_name: ct_portfolio
    restart: always
    network:
      - proxy_network

networks:
  proxy_network:
    external: true
